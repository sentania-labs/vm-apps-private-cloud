# Diagrams

Architecture diagrams for this workspace are authored in Excalidraw format and rendered to PNG.

## Regenerate

```bash
cd docs/diagrams && uv run render.py
```

Source files: `*.excalidraw`
Rendered output: `*.png`
